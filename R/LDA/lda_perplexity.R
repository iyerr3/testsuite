
## compute lda perplexity

library(topicmodels)

data("AssociatedPress", package = "topicmodels")

eval.lda.append.results <- function (data.set,
                                     sql.path = "~/workspace/testsuite/dataset/sql/",
                                     data.path = "../data/",
                                     py.path = "../utils/")
{
    dat <- 
}
